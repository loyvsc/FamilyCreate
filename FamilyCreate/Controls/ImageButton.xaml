<UserControl x:Class="FamilyCreate.Controls.ImageButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" x:Name="MyUserControl"
             d:DesignHeight="35" d:DesignWidth="120">
    <Button x:Name="btn" Command="{Binding Path=ButtonCommand,ElementName=MyUserControl}" CommandParameter="{Binding ElementName=MyUserControl,Path=ButtonCommandParameter}">
        <StackPanel Orientation="Horizontal">
            <Image x:Name="img" Source="{Binding Path=ImageSource, ElementName=MyUserControl}"/>
            <TextBlock x:Name="txtb" Text="{Binding Path=ButtonText,ElementName=MyUserControl}"/>
        </StackPanel>
    </Button>
</UserControl>
